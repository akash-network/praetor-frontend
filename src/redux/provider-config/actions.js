const actions = {
  SET_STATE: 'providerConfig/SET_STATE',
  BECOME_PROVIDER: 'providerConfig/BECOME_PROVIDER',
  BID_ENGINE_CUSTOMIZATION: 'providerConfig/BID_ENGINE_CUSTOMIZATION',
}

export default actions
